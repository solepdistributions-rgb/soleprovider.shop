{% comment %}
  Collection Filter Bar — REMOVED
  This custom filter bar used filter.p.tag URLs which don't actually filter in Shopify.
  Native Shopify sidebar filters (via facets.liquid) are used instead.
  Per-collection filter hiding is handled in facets.liquid.
{% endcomment %}
